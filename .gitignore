.pio
.idea
.DS_Store
.vscode
open-x4-sdk
fs_
fs_*/
lib/EpdFont/fontsrc
lib/I18n/I18nKeys.h
lib/I18n/I18nStrings.h
lib/I18n/I18nStrings.cpp
*.generated.h
.vs
build
**/__pycache__/
/compile_commands.json
/.cache
.history/
/.venv
/.venv-study
*.local*
*.cpfont
# ...except the browser demo's card, which is a shipped asset rather than a
# build artifact, exactly like tools_local/wasm/sdcard/xkcd/images.dat next to
# it. Without this a fresh worktree rebuilds the web card 1.3MB lighter and the
# Study app draws no Chinese at all -- silently, because nothing fails.
!tools_local/wasm/sdcard/study/fonts/KaiTi/*.cpfont
lib/EpdFont/scripts/downloaded_fonts/
lib/EpdFont/scripts/instanced_fonts/
lib/EpdFont/scripts/output/
# Claude Code: share project skills with the team, keep local agent state
# (worktrees, scheduled-task locks, settings.local, scout CLEANUP.md) out.
.claude/*
!.claude/skills/
/managed_components
/.dummy
/CMakeLists.txt
/dependencies.lock
/sdkconfig.*
sdkconfig.sticky
sdkconfig.x4pro

# fork-local: simulator screenshots and logs (scripts_local/)
qa-artifacts/

# The browser build's output is deliberately NOT ignored. It is generated (by
# tools_local/wasm/build.py, from tracked sources) and it is 9MB, both of which
# argue for ignoring it -- but Vercel has no Emscripten, so a git-connected
# deploy that cannot see these files ships a site whose headline feature 404s.
# Committing it is the cheaper failure mode. If the history size ever becomes
# the problem, ignore it again and deploy with `cd site && vercel` instead,
# which uploads the working directory rather than the commit.

# Tool cache. It holds an account id and an email address and has no business
# in a public repo.
.cloudflare/
.ruff_cache/

# upstream (1.6.0rc): nix flake outputs
flake.*

# r8 local build/release artifacts
/dist/
/build_artifacts/

# Local firmware build, test, and inspection artifacts
/.pio-cache/
/work/
/outputs/
/r11_2_work/
/r11.4-x4pro-nimble-forced-legacy-fix/
/*.log
/*.zip
/x4pro-build-error.txt
/reader-indicator-*.txt

# One-off source snapshots created while iterating locally
*.bak
*.r12bak
*.pre-button-grid
